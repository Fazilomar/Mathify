import React from 'react';
import ProofsPage from './ProofsPage';

// StudioPage is the formal Proof & Formula Studio, fully migrated with step-by-step
// mathematical derivations, interactive LaTeX symbol palette, axiom citations,
// and peer-reviewed theorem publishing.
export function StudioPage() {
  return <ProofsPage />;
}

export default StudioPage;
